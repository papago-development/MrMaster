import { CunkiesiriPipe } from './cunkiesiri.pipe';

describe('CunkiesiriPipe', () => {
  it('create an instance', () => {
    const pipe = new CunkiesiriPipe();
    expect(pipe).toBeTruthy();
  });
});
