import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GridarticolepctdelucruPage } from './gridarticolepctdelucru.page';

describe('GridarticolepctdelucruPage', () => {
  let component: GridarticolepctdelucruPage;
  let fixture: ComponentFixture<GridarticolepctdelucruPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(GridarticolepctdelucruPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
