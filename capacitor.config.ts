import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.mrcorny.master',
  appName: 'McCornyMaster',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
